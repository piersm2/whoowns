{
  "name": "rhtp-navigator",
  "version": "0.1.0",
  "private": true,
  "description": "Rural Health Transformation Program navigator for small rural hospitals: application readiness, narrative and budget drafting, post-award compliance, and program reference.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm test -w server",
    "seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
